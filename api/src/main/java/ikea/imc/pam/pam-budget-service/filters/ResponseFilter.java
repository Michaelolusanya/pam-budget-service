package org.imc.pam.boilerplate.filters;

// @Order(2)
// @Component
public class ResponseFilter { // extends OncePerRequestFilter {
    /*
     * @Override protected void doFilterInternal(HttpServletRequest req,
     * HttpServletResponse res, FilterChain filterChain) throws ServletException,
     * IOException { System.out.println("\nORDER 2\n"); String path =
     * req.getRequestURI(); String method = req.getMethod(); if
     * (path.equals("/api/web/v1/hello") && method.equals("GET")) {
     * filterChain.doFilter(req, res); }
     *
     * }
     */

}
